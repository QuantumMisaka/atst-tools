# P2 closeout case A: DP relax against a read-only model copy, runtime section
# on so the run takes the isolated-worker path and writes a sidecar.
calculation:
  type: relax
  init_structure: /home/galileo-group/galileouser02/atst-p2-closeout-20260922/fixtures/init_H2-Au.stru
  fmax: 0.05
  max_steps: 4
  optimizer: FIRE
  trajectory: relax_ro_a.traj

calculator:
  name: dp
  dp:
    model: /home/galileo-group/galileouser02/atst-p2-closeout-20260922/models-ro/model.ckpt-100000.pt

runtime:
  devices: [0]
  binding: inherit
  threads: auto
  telemetry: true
